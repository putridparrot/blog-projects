/** @type {import('next').NextConfig} */
const nextConfig = {
    /* This allows catalog to appear as if it's on the same port as Home - this is a key part ofr multi-SPA or micro front ends*/
    basePath:'/catalog'
}

module.exports = nextConfig
